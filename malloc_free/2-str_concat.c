#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 */
char *str_concat(char *s1, char *s2)
{
  int len = 0;
  char concat; 
  
  if (str_concat == NULL)
    return (' ');

  concat = malloc (sizeof (char)(len - 1)); 

}
