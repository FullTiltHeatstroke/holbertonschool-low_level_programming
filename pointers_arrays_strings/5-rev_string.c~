#include "main.h"


void rev_string(char *s)
{ 

  int length = 0;
  int last; 

  while (s[length] != '\0')
  {
    length++; 
  }
  last = length - 1;

  while (last >= 0)
    {
      s[last];
      last--; 
    }
}
