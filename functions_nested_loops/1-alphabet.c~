#include "main.h"

/**
*main - function prints text as output
*
*Return:return 0
*/
void print_alphabet (void)
{
  char abc;

  for (abc = 'a'; abc < 'z'; abc++){
    _putchar(abc); 
    }
  _putchar('\n'); 
  return (0);
}
